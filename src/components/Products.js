import React from "react";

const Products = () => {
    const products = [
        {
            name: "Kitten time",
            price: 29.50
        }
    ];

    return (
        <div className="products">
            product
        </div>
    )
}

export default Products;